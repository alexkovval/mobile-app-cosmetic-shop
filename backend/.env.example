DATABASE_URL="postgresql://cosmetics:cosmetics@localhost:5432/cosmetics"
JWT_SECRET="dev-secret-change-me"
PORT=4000
# Used only by prisma/seed.ts to build each product's imageUrl (served from
# this same backend's /images route — see app.ts). Leave unset for local
# dev; only change it if the mobile client reaches this API at a different
# host (e.g. a real device on your LAN instead of a simulator).
API_PUBLIC_URL="http://localhost:4000"
