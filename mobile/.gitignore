node_modules
.expo
dist
web-build
*.log
.env

# Generated by `npx expo prebuild` — regenerate with that command rather
# than committing them. Only stop ignoring these if you hand-edit native
# code (a custom native module, a manual Podfile/Gradle change) that
# prebuild wouldn't reproduce.
ios
android
