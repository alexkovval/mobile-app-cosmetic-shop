export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
};

// Tighter corners than before, matching the web edition's Tailwind
// rounded-md (buttons/inputs) / rounded-lg (cards/images) — the old values
// (8/12/16) read as noticeably "softer" than the flat web look.
export const radii = {
  sm: 4,
  md: 6,
  lg: 8,
  pill: 999,
};
