# Read via expo-constants (app.json "extra.apiUrl") for simplicity in this MVP;
# override here if you need a different API host (e.g. a physical device on
# your LAN instead of the simulator, which needs your machine's LAN IP instead
# of localhost).
EXPO_PUBLIC_API_URL=http://localhost:4000
